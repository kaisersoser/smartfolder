# smartfolder GUI Theme

This module is the v2 design-system foundation. It maps the attached design system into egui-friendly tokens for colors, spacing, typography, and reusable widget styling.

The first implementation slice keeps existing screens functional while moving styling decisions into this module. Later wizard milestones should consume these helpers instead of adding one-off colors or spacing in screen code.
